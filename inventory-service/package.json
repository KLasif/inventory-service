{
  "name": "inventory-service-cds",
  "version": "1.0.0",
  "description": "Generated by cds-services-archetype",
  "license": "ISC",
  "repository": "<Add your repository here>",
  "devDependencies": {
    "@cap-js/sqlite": "^2.1.3",
    "@sap/cds-dk": "^9.5.0",
    "mbt": "^1.2.24",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "@cap-js/hana": "^2.6.0"
  },
  "sapux": [
    "app/inventory-ui"
  ],
  "scripts": {
    "undeploy": "cf undeploy inventory-service --delete-services --delete-service-keys --delete-service-brokers",
    "build": "rimraf resources mta_archives && mbt build --mtar archive",
    "deploy": "cf deploy mta_archives/archive.mtar --retries 1"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "hana"
      }
    },
    "[hybrid]": {
      "auth": {
        "kind": "mocked",
        "users": {
          "admin": {
            "password": "admin",
            "roles": ["InventoryManager", "SystemAdmin"]
          },
          "viewer": {
            "password": "viewer",
            "roles": ["InventoryViewer"]
          }
        }
      }
    }
  }
}
